
<p id="notice"><%= notice %></p>

<div class="row">
  <div class="span6 offset3">
    <%= form_for [:admin,@user] do |f| %>

        <% if @user.errors.any? %>
            <div id="error_explanation">
              <h2><%= pluralize(@user.errors.count, "error") %> prohibited this schedule from being saved:</h2>

              <ul>
                <% @user.errors.full_messages.each do |msg| %>
                    <li><%= msg %></li>
                <% end %>
              </ul>
            </div>
        <% end %>



        <%= f.label :email %>
        <%= f.text_field :email %><br/>

        <%= f.label :password %>
        <%= f.password_field :password %><br/>

        <%= f.label :password_confirmation, "Confirmation" %>
        <%= f.password_field :password_confirmation %>

        <%= f.label :isadmin, "Admin" %>
        <%= f.check_box :isadmin %>
        <br/>
        <%= f.submit "Add Admin", class: "btn btn-large btn-primary" %>
    <% end %>
  </div>
</div>